<!DOCTYPE html>
<html lang="en">

<head>
    <title><%= title %></title>
    <!--applying internal styles-->
    <% include ../shared/head %>

    <!--applying internal styles-->
    <style>
        body{
            background-color: rgb(30, 66, 44);
            color: rgb(214, 208, 208);
        }
        .page-header{
            color: rgb(245, 239, 239);
        }
        .form-horizontal{
            padding-left: 20px;
            padding-right: 20px;
        }
        .btn-default, a{
            letter-spacing: 1px;
        }
        .bg{
            margin-top: 10%;
            background-color: black;
            opacity: 0.8;
        }
    </style>
</head>

<body >
    
    <div class="container">                
        <div class="row">
            <div class=" bg col-md-6 col-md-offset-3">
                <div class="page-header">
                    <h3><%= title %></h3>
                </div>
                 <!--login form starts-->
                <form id="" method="POST" action="/login" class="form-horizontal">
                    <div class="form-group">
                        <label for="username">Username</label>
                        <input type="text" name="username" placeholder="Username" class="form-control" />
                    </div>
                    <div class="form-group">
                        <label for="password">Password</label>
                        <input type="password" name="password" placeholder="Password" class="form-control" />
                    </div>
                    <div class="form-group">
                        <button type="submit" class="btn btn-default">Submit</button>
                        <a href="/register">&nbsp Need to Register?</a>
                    </div>
                    <div class="<%= alertClass %>">
	                    <% if (status == 401) { %>
	                        <h5><%= message %></h5>
	                    <% } else { %>
	                        <h5><%= message %></h5>
	                    <% } %>
                    </div> 
                </form>
            </div>
        </div>
    </div>
</body>
</html>